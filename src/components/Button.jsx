import React from "react";
// import './index.scss'

 export default function Button({click}) {
    return (
        <div>
            <button onClick={click}>
                welcome
            </button>
        </div>
        
    )
}